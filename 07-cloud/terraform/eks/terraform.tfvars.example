# Copy file này thành terraform.tfvars và chỉnh giá trị
# cp terraform.tfvars.example terraform.tfvars

aws_region   = "ap-southeast-1"
cluster_name = "my-eks"

# Tùy chọn: đổi phiên bản K8s, VPC, subnet
# kubernetes_version = "1.28"
# vpc_cidr           = "10.0.0.0/16"

# Node: instance type và autoscaling
node_instance_types = ["t3.medium"]
node_min_size       = 1
node_max_size       = 3
node_desired_size   = 2

# Tag (tùy chọn)
# tags = { Environment = "dev", Project = "k8s-learn" }
